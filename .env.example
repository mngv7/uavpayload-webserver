# Copy this file to .env at the repo root and adjust values.

# Sensor WebSocket server bind/port
SENSOR_WS_HOST=0.0.0.0
SENSOR_WS_PORT=8765

# Sensor UDP listener bind/port
SENSOR_UDP_HOST=127.0.0.1
SENSOR_UDP_PORT=6001

# Video WebSocket server bind/port
VIDEO_WS_HOST=0.0.0.0
VIDEO_WS_PORT=8766

# Video UDP listener bind/port
VIDEO_UDP_HOST=127.0.0.1
VIDEO_UDP_PORT=5000

